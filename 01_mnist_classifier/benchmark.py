"""Benchmark — baseline vs optimized. The proof.

Measure size (MB), latency (ms/img), and test accuracy for both models and
print the comparison table. Warm up before timing; average over many images.
"""

# TODO(you): load both models, measure size/latency/accuracy, print the table.
