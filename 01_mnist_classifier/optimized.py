"""Optimized MNIST classifier — the baseline, whittled with int8 quantization.

Load the trained baseline, apply post-training quantization (start dynamic),
and keep accuracy within tolerance. See README for the concepts to nail first.
"""

# TODO(you): load baseline weights, quantize, save + expose the int8 model.
