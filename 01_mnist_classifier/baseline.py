"""Baseline MNIST classifier — the standard PyTorch CNN.

Train a small convolutional net to ~99% test accuracy. The architecture is
yours to design (see README). Keep it small; a 2-conv-block net is plenty.
"""

# TODO(you): imports, model, data loaders, train loop, eval, save weights.
